CREATE TABLE plant(
    type TEXT NOT NULL,
    name TEXT NOT NULL,
    color TEXT NOT NULL,
    flower  TEXT NOT NULL,
    seed TEXT NOT NULL,
    fruit TEXT NOT NULL,
    spore TEXT NOT NULL,
id_plant SERIAL NOT NULL

);
